import { View } from 'react-native'

// Placeholder screen for the FAB tab slot — never actually displayed
// The FAB button in the tab bar opens a modal instead
export default function FabPlaceholder() {
  return <View />
}
